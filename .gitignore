# python
__pycache__/
*.pyc

# stipple build outputs
stipple_out/
out_*/
bbc/data/.build_tmp/

# beebasm listing + symbol dump (regenerated on every build)
bbc/*.lst
bbc/*.labels

# raw beebasm SAVE output (no -do) -- intermediate for 4k-emu/bin_to_h.py
bbc/STIP256

# tooling binary (download separately)
tools/beebasm.exe

# editor / IDE
/.vscode/
/.vs/
/.claude/
